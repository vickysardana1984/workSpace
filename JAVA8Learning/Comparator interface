Comparator :

It is used for custom sorting.While defining sorting JVM will always use Comparator compare method.
It returns int value. 

public int(obj1,obj2) {
 return -1 ; If obj1 has to come before obj 2
 return 1 ; If obj1 has to come after obj 2
 return 0 ; if obj1 and obj2 are equal.
 
}